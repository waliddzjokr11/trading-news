name: Crypto Monitor

on:
  schedule:
    - cron: '*/30 * * * *'   # every 30 min — private repo safe (1440 runs/mo < 2000 limit). Use '*/15 * * * *' if public.
  workflow_dispatch:           # manual trigger from GitHub UI

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 8        # kills job if hangs — saves free minutes
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run monitor
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          CRYPTOPANIC_API_KEY: ${{ secrets.CRYPTOPANIC_API_KEY }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
        run: python src/main.py

      - name: Keep-alive webhook (Render free tier)
        run: |
          if [ -n "${{ secrets.WEBHOOK_URL }}" ]; then
            echo "Pinging webhook health..."
            curl -fsS "${{ secrets.WEBHOOK_URL }}/health" || echo "keep-alive failed (webhook may be starting)"
          else
            echo "No WEBHOOK_URL secret — skipping keep-alive"
          fi

      - name: Validate state before commit
        run: |
          python -c "
          import json, sys
          try:
              with open('state.json') as f:
                  s = json.load(f)
              print('state.json valid. run_count:', s.get('run_count', 0))
          except Exception as e:
              print('state.json INVALID:', e)
              sys.exit(1)
          "

      - name: Commit state and dashboard
        run: |
          git config user.name "crypto-monitor-bot"
          git config user.email "bot@github-actions"
          git add state.json dashboard/index.html logs/
          git diff --staged --quiet && echo "No changes to commit" && exit 0
          git pull --rebase origin main || true
          git commit -m "chore: update state [skip ci]"
          git push || (git pull --rebase origin main && git push)

      - name: Notify on failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="⚠️ crypto-monitor GitHub Actions run FAILED. Check: https://github.com/${{ github.repository }}/actions" \
            || true
